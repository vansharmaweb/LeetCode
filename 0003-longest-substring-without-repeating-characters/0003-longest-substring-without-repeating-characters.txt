/*
 * Problem: 1695. Maximum Erasure Value (#3)
 * Difficulty: Medium
 * Topics: Uncategorized
 * Language: Sort by:
 * LeetCode URL: https://leetcode.com/problems/longest-substring-without-repeating-characters/
 * Solved on: 2026-09-22
 * Runtime: 0 ms | Memory: 19.19
MB
 */

class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        unordered_map<char, int> lastSeen; // char -> last index
        int maxLen = 0, start = 0;

        for (int i = 0; i < s.size(); i++) {
            if (lastSeen.count(s[i]) && lastSeen[s[i]] >= start) {
                start = lastSeen[s[i]] + 1;
            }
            lastSeen[s[i]] = i;
            maxLen = max(maxLen, i - start + 1);
        }
        return maxLen;
    }
};

