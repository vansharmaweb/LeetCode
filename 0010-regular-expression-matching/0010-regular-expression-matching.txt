/*
 * Problem: 44. Wildcard Matching (#10)
 * Difficulty: Hard
 * Topics: Uncategorized
 * Language: Sort by:
 * LeetCode URL: https://leetcode.com/problems/regular-expression-matching/
 * Solved on: 2026-09-22
 * Runtime: 0 ms | Memory: 8.98
MB
 */

        for (int i = 1; i <= m; i++) {
            for (int j = 1; j <= n; j++) {
                if (p[j - 1] == '.' || p[j - 1] == s[i - 1]) {
                    dp[i][j] = dp[i - 1][j - 1];
                } else if (p[j - 1] == '*') {
                    // zero occurrence of preceding element
                    dp[i][j] = dp[i][j - 2];
                    // one or more occurrence, if char before * matches
                    if (p[j - 2] == '.' || p[j - 2] == s[i - 1]) {
                        dp[i][j] = dp[i][j] || dp[i - 1][j];
                    }
                }
            }
        }
        return dp[m][n];
    }

