/*
 * Problem: 25. Reverse Nodes in k-Group (#24)
 * Difficulty: Medium
 * Topics: Uncategorized
 * Language: Sort by:
 * LeetCode URL: https://leetcode.com/problems/swap-nodes-in-pairs/
 * Solved on: 2026-09-22
 * Runtime: 0
ms | Memory: 11.07
MB
 */


        while (prev->next && prev->next->next) {
            ListNode* first = prev->next;
            ListNode* second = first->next;

            // swap
            first->next = second->next;
            second->next = first;
            prev->next = second;

            // move prev ahead by 2 (now pointing to 'first', the new second in
            // the swapped pair)
            prev = first;
        }
    }
        return dummy.next;

